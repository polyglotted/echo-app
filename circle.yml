machine:
  services:
    - docker
  java:
    version:
      oraclejdk8
deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push polyglotted/echo-app